/* ============================================================
   Aventurs — Npc
   Interacciones con NPCs según rol.
   Se completa en su Fase correspondiente.
   ============================================================ */

(function (A) {
  'use strict';

  const Npc = {
    // API se completa en Fase 1+
  };

  A.Npc = Npc;
})(window.Aventurs);
