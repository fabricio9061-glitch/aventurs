/* ============================================================
   Aventurs — Spellbook
   Aprender, listar y lanzar hechizos.
   Se completa en su Fase correspondiente.
   ============================================================ */

(function (A) {
  'use strict';

  const Spellbook = {
    // API se completa en Fase 1+
  };

  A.Spellbook = Spellbook;
})(window.Aventurs);
