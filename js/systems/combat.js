/* ============================================================
   Aventurs — Combat
   Combate por turnos. Lógica pura sin DOM.
   Se completa en su Fase correspondiente.
   ============================================================ */

(function (A) {
  'use strict';

  const Combat = {
    // API se completa en Fase 1+
  };

  A.Combat = Combat;
})(window.Aventurs);
